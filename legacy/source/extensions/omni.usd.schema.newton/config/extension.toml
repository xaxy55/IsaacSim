[core]
reloadable = false
# Load at the start, load all schemas with order -100 (with order -1000 the USD libs are loaded)
order = -100

[package]
version = "1.2.0"
category = "Simulation"
title = "Omni USD Newton Schema"
description="Extension used to host all Newton USD schemas."
keywords = ["simulation", "usd", "schema"]
changelog = "docs/CHANGELOG.md"
readme = "docs/Overview.md"
preview_image = "data/preview.png"
icon = "data/icon.png"
writeTarget.kit = true
writeTarget.python = true
writeTarget.usd = true

[dependencies]
"omni.usd.libs" = { }

[[python.module]]
name = "usd.schema.newton"

[documentation]
pages = ["docs/Overview.md","docs/USD_SCHEMAS.md", "docs/CHANGELOG.md"]
